<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.davizn.adminInterface.adminDAO">
	
	

	<!-- <select id="dayCount">
		select to_char(datacreate,'DAY') as "day",
		count(*) as "daycount" from personaldata group by to_char(datacreate,'DAY')
	</select>
	

	<select id="monthCount">
		select  to_char(datacreate,'MM') as "month",
		count(*) as "monthcount" from personaldata group by  to_char(datacreate,'MM')
	</select>
	

	<select id="hourCount">
		select to_char(datacreate,'HH') as "hour", 
		count(*) as "hourcount"  from personaldata group by to_char(datacreate,'HH')
	</select>
	

	<select id="dataDaySum">
		select to_char(datacreate,'DAY') as "day",
			   count(to_char(datacreate,'DAY')) as "daycount",
			   sum(count(*)) over (order by to_char(datacreate,'DAY') 
			   asc rows between unbounded preceding and current row) as "totalcount"
			   from personaldata
			   group by to_char(datacreate,'DAY')
	</select>
	

	<select id="dataDaySum">
		select to_char(datacreate,'HH') as "day",
			   count(to_char(datacreate,'HH')) as "daycount",
			   sum(count(*)) over (order by to_char(datacreate,'HH') 
			   asc rows between unbounded preceding and current row) as "totalcount"
			   from personaldata
			   group by to_char(datacreate,'HH')
	</select>
	

	<select id="dataDaySum">
		select to_char(datacreate,'MM') as "day",
			   count(to_char(datacreate,'MM')) as "daycount",
			   sum(count(*)) over (order by to_char(datacreate,'MM') 
			   asc rows between unbounded preceding and current row) as "totalcount"
			   from personaldata
			   group by to_char(datacreate,'MM')
	</select> -->
	 
	
	
	
	
	
</mapper>